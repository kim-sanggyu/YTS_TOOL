# 세션 핸드오프

> 다른 PC/새 세션에서 이어갈 때 이 문서부터 읽는다. 로컬 메모리(`~/.claude/.../memory`)는 PC 간 이어지지 않으므로, **PC를 넘나드는 컨텍스트는 여기(git)에 남긴다.** 세션 끝마다 이 문서를 갱신.

---

## 최신: 2026-07-19 (origin/master = `63e0931`)

### 이번 세션 한 것 (7커밋, 전부 push 완료)
| 커밋 | 내용 |
|---|---|
| `626ea3f` | YTS 연도조회 `PAY_WRK_MAIN.YY` 전환(list 9종, main·calc 교집합) + 기타 그룹 전송사용액 + 장기주택저당 라벨 |
| `11125b7` | 리스트 행 배경색 개편 — 오류=주황 / 선택(본행클릭)=파랑 / 일치·불일치·미실행=무색 |
| `213a271` | 그밖의소득공제 4항목 전송·대조 배선 |
| `bc93e0f` | 그밖의소득공제 그룹 등록 + 현황판 원천 갱신 |
| `38d49d6` | 기타세액공제 그룹 등록 |
| `f1a62d7` | 계산과정 팝업(좌 계산과정 + 우 실행과정) |
| `63e0931` | 계산과정↔실행과정 불일치 대조(적/청) + NTS IN/OUT 전체 표시 |

### 핵심 결과물
- **그밖의소득공제 4항목**(라이브캡처 실측확정): 우리사주 `8452`(MAIN.STOCK_URDM→OTO_SU, 전액/한도1500만) · 장기집합 `8451`(PEN 562-100→OTO_LONG_STOCK_SAVING, ×40%/240만) · **청년형 `8501`**(PEN 562-140→OTO_YM_LONG_STOCK_SAVING, ×40%/240만; 카탈로그 미등재였음) · 고용유지 `8453`(MAIN.EMPL_MTN_WAGE_CUT→OTO_EMPL_MTN_WAGE_CUT, ×50%/1000만). useAmt self, 소계 8921. → 기타 드롭다운 "그밖의소득공제" 그룹.
- **기타세액공제 3항목**: 외국납부 `8751` · 주택차입금이자 `8752` · 납세조합 `8753` → 기타 드롭다운 "기타세액공제" 그룹(이미 send:true였고 조회뷰만 추가).
- **장기주택저당 8321~8329**: 국세청 화면 기준 2011이전(5행)/2012이후(4행) 2구분으로 라벨 정정 + 한도괄호 제거, 카탈로그 동기화. (카탈로그의 3구분 2015이후는 수기 오류였음 — 되돌리지 말 것)
- **계산과정↔실행과정 팝업 대조**(플래그십): 계산과정 버튼 → 중앙 팝업(좌 CALC_PROC_TOTAL / 우 실행과정 DetailView). 실행과정에 NTS 원본 IN/OUT 전 필드 표(NtsIoRow) + 코드별 YTS공제(ytsDdcMap=resultCol) ↔ NTS공제 대조색(불일치 적/일치 청/✗✓). 계산과정 텍스트도 라인 하이라이트(procLineClass). 실행과정 표는 계산과정 등장순서로 정렬(procCodeOrder).

### ▶ 다음 할 일
1. **값검증** — 그밖의소득공제·기타세액공제 그룹 대상자로 전체실행, 원단위 일치 확인(특히 한도캡 240/1500/1000만 초과 시 NTS 자체캡 동작).
2. **계산과정 라벨맵 확장** — `HometaxCalcPanel.tsx`의 `PROC_LABEL_CODE`에 소계형(카드·의료·교육·기부·투자조합) 편입 → 계산과정 순서·하이라이트·대조 확대.
3. baseline 감지기(도구의 "두 얼굴 ②").

### ⚠️ 주의
- **새 필드(`ytsDdcMap`·`ntsIn`·`ntsOut`)는 재실행해야 채워짐** — 기존 캐시엔 없으니, 실행과정 IN/OUT 표·YTS 대조색을 보려면 "지우기 → 재실행".
- 8751·8752·8753은 X2026 대상자 0명이라 원단위 미검증.
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout·.next/types) — 이번 작업과 무관, 원래 있던 것.

### 방법론(고정)
- NTS 계약 분석은 **라이브 캡처**로: `node docs/hometax-capture-io.mjs`(입력·계산) → `--parse`(증분 diff). 상세 `docs/nts-contract-capture-method.md`. 매 세션 새 방법 만들지 말 것.
- 컬럼 존재/테이블 확인은 일회성 스크립트 + `node --env-file=.env.local <script>.mjs`.

### 이어가기 절차 (다른 PC)
1. `git pull` (origin/master = `63e0931` 이상)
2. 이 문서 읽기
3. `npm run dev`로 화면 확인 시 로그인 필요(아이디/비번)
