# 세션 핸드오프

> 다른 PC/새 세션에서 이어갈 때 이 문서부터 읽는다. 로컬 메모리(`~/.claude/.../memory`)는 PC 간 이어지지 않으므로, **PC를 넘나드는 컨텍스트는 여기(git)에 남긴다.** 세션 끝마다 이 문서를 갱신.

---

## 최신: 2026-07-27 세션2 — 검증 백로그 #1·#2 소진 (소스변경 0, origin/master=`6bc446f`)

### 이번 세션 한 것 (검증만 — 커밋할 소스 변경 없음)
- **백로그 #1 (기타세액공제 8751/8752/8753 + 그밖의소득공제 8451/8452/8501/8453) = 실대상자 0명으로 종결.** 2025 전수 조회(`docs/find-etc-other-2025.mjs`, 읽기전용 SELECT) → **양 그룹 모두 대상자 0명**(FRGN_PAY_TAX·HOUSE_ALR·ASSO_SUB_TAX_AMT·STOCK_URDM·EMPL_MTN_WAGE_CUT·562-100/140 전부 0행, 컬럼은 실재=쿼리 무에러). 계약·배선은 이미 합성 라이브캡처로 **`확정`**(매핑 7행 send:true·resultCol 연결·컬럼 실재 정적확인, 메모리 `reference_nts_etc_credit_8751`·`reference_nts_other_income_8451`가 status:추정으로 남아있으나 **코드는 확정이 최신**). → **"실데이터 미발생, 계약확정으로 종결"** — 첫 대상자 발생 시 자동 대조되도록 배선은 살아있음. 파인더 재사용 가능.
- **백로그 #2 (보험료 원본전송 8710/8711 전체실행 값검증) = ✅ 완료.** 07-27 캐시(`data/hometax-batch-results/2025-nts2025.json`, savedAt 07-27, 485명 전원 result, **07-25 원본전송 전환 이후**)로 국세청 OUT(`ntsMap[code]`) ↔ YTS `RT_IF_*`(`ytsDdcMap[code]`) 전수대조 — **8710(416명)·8711(3명) 전원 완전일치**(불일치0·절사차0·null0). **cap 실제작동 대량**(8710 100만cap 도달 335명/미만 81명, 8711 2명)에도 0불일치 → 국세청 100만 self cap 독립재현이 YTS 엔진 cap과 원단위 일치, **한도로직 교차검증 성립**(자명한 sub-cap 일치 아님). 메모리 `reference_nts_insurance_8710` "값검증 미완"→완료 갱신.

### ⚠ 주의
- 소스 변경 없음(파인더 스크립트 1개 신규 + 메모리 1건 갱신뿐). **커밋 불요**(원하면 `docs/find-etc-other-2025.mjs`만 add).
- 메모리 `reference_nts_etc_credit_8751`·`reference_nts_other_income_8451`의 `status:추정`은 실코드(`mapping/2025.ts`)에선 이미 `확정` — 문서 지연, 코드가 소스오브트루스.

### ▶ 다음 할 일 (검증 백로그, 우선순위 — #1·#2 소진 후)
1. (인프라) **커버리지 맵 감사**(전 항목 원본/서버 vs 중간값/화면 분류) · **의료비·교육비 화면구동 검증화면**(L03 서버 미보유 로직=국세청 화면JS, `medi-popup-auto.mjs`식 반자동) · **골든마스터 회귀검증**.
2. (오픈후) 전체실행 SSE 스트리밍 · baseline 변경감지 트립와이어 · 계약변경 감지기 · yts필드↔amtClusCd 분리매핑 카탈로그.
3. (데이터 발생 시) 기타세액공제·그밖의소득공제 첫 실대상자 나오면 파인더로 감지 후 원단위 대조.

---

## 2026-07-27 세션1 — 고향사랑 8784 규명 + ③판정 사각·헤더 UI + ④오탐 + 세션창 최소화 (origin/master=`6bc446f`)

### 이번 세션 한 것 (커밋 4건, push 완료)
| 커밋 | 내용 |
|---|---|
| `ef0355b` | fix: ③표 판정 "YTS없음 vs NTS값" 사각 정정 — 기부금 코드 한해 undefined→0 대조(`GIFT_CODES`). 8784 규명 프로브 2종 |
| `71a70cb` | feat: 비교패널(①③) 헤더 — 색범례 삭제·'불일치 N건' 배지, `giftCmp` 판정 단일화, `rightExtra`로 전체/값보기 우측정렬, 타이틀 행 클릭=접기/펼치기 |
| `ba81120` | fix: ④ 미전송 8003(부양가족 통합) 오탐 제거 — `altSent` 대체전송 마커 |
| `6bc446f` | feat: 국세청 세션 창 오프스크린→화면내 최소화(작업표시줄 클릭 복원) |

### 핵심 결과물 — 고향사랑 8783/8784 "1원" 완전 규명
- **국세청 자체 1원 불일치**(메모리 [[reference_nts_hometown_love_8783_8784]]): 로스터(계산과정)=고향사랑 총액 **55,296**(대상금액 60,825×100/110) vs 코드회신(입력화면/L03)=**8783(55,295 내림)+8784(1 절사잔여)**. 특별 지출0/미전송이어도 국세청이 8784=1 스스로 생성(A/B 실측: `omitCodes:["8784"]`로 빼도 1 유지). 원인=국세청이 대상금액 먼저 산출→×100/110 반올림손실(입력화면도 "소수점 계산차이" 자백). **우리값(GIFT_SUB_AMT)=55,296=로스터 일치**. self냐 합이냐 문제 아닌 국세청 내부 비일관 → **1원 자체는 특수케이스로 현행 유지**(상규님 판정).
- **③표 판정 사각 정정**: `ytsDdcMap[code]` undefined면 대조 스킵하던 것(8784=YTS0 vs NTS1인데 ✗ 안 뜸)을 **기부금 코드에 한해** 0으로 대조(`giftCmp` 헬퍼, 렌더+불일치건수 공유). 블랭킷 `??0`은 부양가족 유형별(8004~09, 171+161명)·투자(8420) 등 per-code 미추적 항목을 330+명 거짓양성으로 만듦 → `GIFT_CODES`로 격리. 전수 triage(`docs/gift-8784-triage.ts`, 배치 스냅샷 오프라인 분석)로 **8784만 신규✗** 확인.
- **④ 미전송 8003 오탐 제거**: 부양가족(통합)은 IN을 8004~09 유형별로 대체 전송(send:false)이나 값 보유라 ④에 상시(485명 중 233명) 뜨던 거짓경보 → `MappingRow.altSent` 마커로 ④에서 제외(하드코딩 대신 매핑 마커).
- **세션 창 UX**: 오프스크린(-10000) → 화면내 생성 + CDP(`Browser.setWindowBounds`) 즉시 최소화(작업표시줄, 클릭 복원). 최소화 상태에서도 세션수립·L03 정상 확인(`docs/nts-session-minimize-check.ts`, 총급여 5천만→결정세액 3,387,500).

### ⚠ 미해결 / 주의
- **기존 배치 스냅샷은 재실행 전까지 옛 결과 유지** — 8784 ✗·8003 ④제외는 대상자 재실행/전체실행해야 반영(`ytsDdcMap`·`missing`은 실행시점 생성).
- `omitCodes`(runHometaxCompare) = 계약 A/B용 프리미티브로 남김. 프로브 3개 남김: `gift-8784-ab-probe.ts`·`gift-8784-triage.ts`·`nts-session-minimize-check.ts`.
- `npm run typecheck` 기존 에러 3~4개(tax-insight·hwp-layout) — 무관.

### ▶ 다음 할 일 (검증 백로그, 우선순위)
1. 기타세액공제 8751/8752/8753 실대상자 검증 · 그밖의소득공제 8451/8452/8501/8453(YTS원천 미확정·mapping 미반영).
2. 보험료 원본전송(8710/8711) 전체실행 값검증.
3. (인프라) 커버리지 맵 감사(원본/서버 vs 중간값/화면) · 의료비·교육비 화면구동 검증화면 · 골든마스터 회귀검증.
4. (오픈후) 전체실행 SSE 스트리밍 · baseline 변경감지 트립와이어 · 계약변경 감지기 · yts필드↔amtClusCd 분리매핑 카탈로그.

---

## 2026-07-26 — ISA 소계형 + 기부금 검증완료 + 세부행 색상 (push 완료)

### 이번 세션 한 것
- **ISA 연금계좌(8707/8708) → 8705 소계형 전환** (커밋 `d03a716`, push완료): 국세청 이중케이스(`X202600349`) 라이브캡처 실측 — IN은 코드별 전송, OUT은 **per-code ddcAmt + 소계 8705(ISA합) + 총합 8706** 모두 반환(계좌별 한도 9M/6M 독립). **YTS는 `RT_ISA_PEN_AMT` 단일 합산컬럼뿐**이라 ③표 per-code 대조 불가(둘 다 non-zero면 8707/8708 각각 합산값과 비교돼 오탐). 기존 "2026-07-15 per-code 확정"은 **한쪽만 non-zero 표본의 사각**이었음. → `outCode=8705` 멤버화, `SUBTOTAL_CODES` 8705 등록, 로스터 앵커 `"ISA연금계좌납입액"`→8705, ③표 **소계 멤버는 입력(IN)만 표시**(카드·의료 동형, 상규님 선택). 연금 드로어 ISA=indigo. 메모리 `reference_nts_isa_pension_8705`. probe `gift-isa-both-probe.mjs`.
- **기부금 ③표 유형별 YTS공제 대조 — 검증완료·커밋** (커밋 `efbf375`, push완료): 세션2의 `injectGiftDdc` 미커밋분을 **Y2025 재실행 검증 후 커밋**. 정치·고향·특례·종교외 당해 + 종교 이월(8821) YTS공제↔NTS ✓. 중복 `(CLS,YY)` 0건(=할당 전제 안전, `gift-verify-roster-probe.mjs`). 이월 색상(`carried`)·로스터 앵커(`GIFT_CARRY_BASE`) 포함. IN/OUT diff 로직 대칭 확인.
- **주택자금 주택임차 원리금(8311/8312) cyan 강조** (커밋 `cd14c6d`, **미push**): `PersonalTable` 세부행 강조를 코드별 헬퍼(`hiText`/`hiCode`)로 정리, 8711(violet)과 함께 처리(리터럴 클래스 유지 — Tailwind JIT 안전).

### 세부행 강조색 팔레트
emerald(정치)·sky(고향)·amber(이월)·teal(난임)·fuchsia(미숙아)·violet(장애인전용보험 8711)·indigo(ISA)·cyan(주택임차 8311/8312).

### ⚠ 미push / 미해결
- `cd14c6d`(주택임차 색상) **로컬만**. `d03a716`·`efbf375`는 push완료(origin/master=`efbf375`).
- 검증 데이터 갭(세액영향 없음): 우리사주 8744·특례이월 8811~15·종교외이월 8831~35은 Y2025 대상자 0.

### ▶ 다음 할 일 (검증 백로그, 우선순위)
1. 기타세액공제 8751/8752/8753 실대상자 검증 · 그밖의소득공제 8451/8452/8501/8453(YTS원천 미확정·mapping 미반영).
2. 보험료 원본전송(8710/8711) 전체실행 값검증.
3. (인프라) 커버리지 맵 감사(원본/서버 vs 중간값/화면) · 의료비·교육비 화면구동 검증화면 · 골든마스터 회귀검증.
4. (오픈후) 전체실행 SSE 스트리밍 · baseline 변경감지 트립와이어 · 계약변경 감지기 · yts필드↔amtClusCd 분리매핑 카탈로그.

---

## 2026-07-25 세션2 — 검증 사각 원리 + 기부금 ③표 (커밋 `efbf375` · push 완료)

### 이번 세션 한 것 (대부분 분석·원리, 코드 1건)
- **★검증 커버리지 원리 정립** (메모리 `project_nts_verification_coverage_model`): "YTS값을 NTS에 전송·비교=완벽검증"은 성립 안 함. **검증깊이 = 경계위치 × 원본여부.** 중간값을 보내면 그 앞단은 NTS가 **에코**(사각). 사각 4종(①중간값에코 ②결과만반영 ③항목없음/다른구현 ④YTS·NTS 공유 세법오해). 단일대조 불가 → **3층 겹치기**(API대조·화면구동·골든마스터, 서로 대체불가). **"원본이냐 중간값이냐는 검증대상 계산의 경계 기준으로 정의"** — 과거 확정값(기부금 전년소진누계)은 정당한 원본입력(에코 아님), 올해 계산의 스텝(교육비 한도·의료비 실손)을 미리 해치워 넘기면 진짜 사각.
- **의료비·교육비 = 화면구동 검증 필요** (메모리 `project_medi_edu_source_verification`): 한도·3%·실손·집계 로직이 L03 **서버가 아니라 국세청 화면 JS**에만 있음(실측). API로 원본 넣어도 서버가 앞단 안 해 사각. → 원천(`FMLY_DTL.MEDI_*`/`EDU_AMT`+`FMLY.EDU_CLS`)으로 **화면구동 반자동**(`medi-popup-auto.mjs`식) 검증화면 = 추가개선 과제. 화면구동이 유일 출구(eversafe headless거부·재구현은 독립오라클 상실). **오라클은 스팟, 전건은 골든회귀.**
- **기부금 = 커버리지 모범 케이스**: `GIFT_ABLE_SUB_AMT`=올해 공제가능액(=원본). 유형×연도별 원본 → 국세청이 **건별 self 공제 반환**(8740/8743/8746/8747/이월 8811~8835) → 건단위 1:1 대조(한도·소진배분까지 포착, 소계대조보다 엄격). 소스 전구간 추적검증 완료(`injectGiftVals`→`buildCompareBody`→`ntsMap`→`GiftTable`). 정치자금(548-020, 8740) 공제식 실측검산: **10만↓ × 100/110 + 초과 × 15%** (120,000→93,909, 112,000→92,709 등). ⚠ `injectGiftVals`는 `=`할당(누적 아님) — 동일 (유형,연차) 중복행 없다는 전제, 감사 때 확인.

### ⚠ 미커밋 1건 (타입체크 통과·**미검증**·재실행 필요)
- **③표 기부금 YTS 공제 빈칸 수정** (`runCompareForCalcNo.ts`): 정치자금 8740 등 `resultCol` 없는 기부금 코드는 ③ 실행과정표 **YTS공제 빈칸**이었음(`RT_POLITIC_FUND`는 초과분 3,000만 담아 부정확). → `injectGiftDdc` 신설: 유형×연도별 `GIFT_SUB_AMT`를 `ytsDdcMap`에 주입, `CompareInput.giftDdc`로 전달(vals와 분리 → `inputHash` 불변·캐시 영향 0). 특례/일반(당해)도 `RT_*` 대신 `GIFT_SUB_AMT`로 통일 → 비교탭과 일치. **`ytsDdcMap`은 실행시점 생성이라 기존 저장결과는 빈칸 그대로 → 대상자 재실행해야 반영·확인.**
- 8741(정치 10만↓ 소계) 참고행 ③표 노출 시도 → **원복**(정치자금 한 줄 유지).

### ▶ 다음 할 일
1. **기부금 ③표 수정 검증 후 커밋**: 정치자금(예 `Y202500143`) 재실행 → YTS공제 93,909·판정 ✓ 확인.
2. **기부금 유형별 검증 계속**: 특례(8743+이월 8811~15)·일반 종교(8746)/종교외(8747)+이월·우리사주(8744)·고향(8783/84).
3. (기존) 보험료 원본전송 전체실행 값검증, 기타세액공제 8751/8752/8753.
4. (추가개선) 의료비·교육비 화면구동 검증화면 — 비교화면 1차 마무리 후.
5. (감사) 매핑표 전 항목을 "원본/서버(안전) vs 중간값/화면(사각)" 분류 → 커버리지 맵.

---

## 2026-07-25 세션1 (push 완료, origin/master = `c9a563d`)

### 이번 세션 한 것 (push 완료)
| 커밋 | 내용 |
|---|---|
| `d6008d8` | 혼인세액공제 원본 500,000 전송 + 출산입양 소계 8761 YTS공제 대조 |
| `64a3b97` | 출산입양 전송 사용액 순번별(첫째·둘째·셋째 인원) 표시 |
| `25237e8` | 장애인전용 보장성보험료(8711) 세부행 강조색(violet) |
| `92c97cc` | 보장성보험료 8710/8711 원본 지출총액 전송 전환 (국세청 100만 self cap 실측) |
| `5d9ae29` | 의료비 난임(teal)·미숙아(fuchsia) 세부행 색상 구분 |
| `c9a563d` | 의료비 부양가족별 NTS 대조 검증도구 4종 + 유형집계 실측확정 |

### 핵심 결과물 — ★원본전송 원칙 대거 적용(한도/소진 로직 교차검증)
- **✅ 혼인 8790 소진캡 실측확정**: 라이브캡처(총급여2천만) — IN ddcAmt=500,000 → **OUT 276,750**(=산출615,000−근로338,250, 잔액). **국세청이 혼인에 잔액 소진캡 독립적용**(맹목에코 아님). → 전송 `RT_MRRG`(소진후값)→**원본 500,000** 전환. 자격판정=`FAM_MRRG`(PAY_WRK_FMLY `MRRG_TAX_SUB_YN='Y'`·배우자550-010 카운트, 소진 무관 원데이터→완전소진 자격자도 포착). 대조=국세청 캡 vs YTS RT_MRRG 엔진캡. `mappingSentValue`·`buildCompareBody`·`injectFamilyVals`·`personal.inputConst`.
- **✅ 출산입양 8761 소계대조 붙임**: 8764~66에 `resultCol=RT_PER_CHI_AMT`(카드 OTO_CARD_ETC·의료 RT_MEDI_AMT 소계형 동형)→`ytsDdcMap[8761]` 대조. 실측 이영화(Y202500414, 둘째·소진) NTS 37,788=YTS 37,788 일치(소계형도 소진캡). + 전송사용액 순번별 표시(`birthBreakdown`, PAY_WRK_FMLY PER_CHI_YN 3/5/7).
- **✅ 보장성보험료 100만 self cap 실측확정**: 구분프로브(`docs/insurance-cap-probe.mjs`) — 우리 전송형태(useAmt=원본·ddcLmtAmt=0)로 원본 20,191,680 전송→**OUT 120,000**(=100만×12%, no-cap 아님). 6건 전원 (A) 국세청 자체 cap. → 전송 `SPCL_IF_*`(100만 capped)→**원본 지출총액**(`PAY_WRK_FMLY_DTL SUM(GRT_INSU/HDC_PERS_INSU)`→`INS_GRT/INS_HDC` 가상컬럼) 전환. `injectInsuranceVals`·isVirtual `INS_`·housingList 표시.
- **✅ 의료비 부양가족별 NTS 대조 완전일치**: `medi-popup-auto.mjs`(반자동)로 YTS 부양가족별(PAY_WRK_FMLY_DTL MEDI_AMT/CA/ISA/HDC_MC/LOSS)을 국세청 팝업 대상자별 그리드에 자동입력→NTS UI 유형집계와 대조. **IN·OUT 전원 ✅**(본인등 7,346,151=본인 8,206,010−실손 859,859 / 난임 2,213,270 / 소계 10,508,481 / 공제 1,573,489). **국세청 UI의 실손차감·본인/그밖의 분류·3%문턱·율 = YTS 완전동일** → 우리 매핑(injectMediVals 유형집계 전송) 정당성 확정. FMLY_DTL 의료비컬럼 코멘트: MEDI_AMT=일반/MEDI_CA=미숙아/MEDI_ISA=난임/MEDI_HDC_MC=본인등특례/MEDI_LOSS=실손.

### ▶ 다음 할 일
1. **보험료 원본전송 전체실행 값검증** — 보장성보험료 대상 전체실행해 NTS OUT ↔ `RT_IF_*` 원단위 일치 확인(구분프로브로 논리는 확증, 배선 경로 회귀 확인용). 혼인·출산입양 재실행 ③표 ✅도.
2. 검증 진행표 남은 그룹(**기타세액공제 8751/8752/8753** 등) 이어가기.
3. 백로그: **실행/지우기 범위 비대칭**(전체실행은 탭별 부분인데 지우기는 전체뿐→부분지우기, [[project_hometax_calc_backlog]] 추가함)·인적공제 OUT갭·집계형 OUT 매핑 한계·yts↔amtClusCd 분리매핑·계약변경 감지기.

### ⚠️ 주의
- **★websquare 자동화 한계**: JS value 세팅·dispatchEvent click 을 websquare가 거부(not enabled/내부모델 미반영). 팝업 그리드 입력은 elementHandle click+fill(사람이 연 팝업), **총급여는 수동입력 필요**(급여 적용→근로소득금액 산출돼야 의료비 3%문턱 계산). `medi-popup-auto.mjs`는 반자동(팝업열기·총급여·본계산만 사람, 나머지 자동). 완전무인은 미달성.
- DB 오늘은 안정(어제 ORA-12170 재발했으나 오늘 정상).
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout) — 무관.
- 방법론 고정: NTS 계약은 라이브캡처(`hometax-capture-io.mjs`)·합성 프로브(`*-probe.mjs`), 팝업 셀렉터 탐색(`medi-popup-explore.mjs`).

---

## 이전: 2026-07-24 (origin/master = `03c36ed` 이후 이 핸드오프 커밋)

### 이번 세션 한 것 (push 완료)
| 커밋 | 내용 |
|---|---|
| `c2f064c` | 세액감면 조특법30조 국세청 통합처리 실측확정 (8603 전송0 팬텀 규명·종료) |
| `03c36ed` | 신규 3그룹(세액감면·보험료·교육비) 전체실행 검증 종료 + 교육비 진단 스크립트(`docs/edu-diag.mjs`) |

### 핵심 결과물 (검증 종료 2건)
- **✅ 2번(8603 팬텀) 종료** — 라이브캡처 #3: 8608(90%)에만 감면대상급여 입력해도 국세청이 감면세액(225,585)을 **8603·8608·8924(세액감면계)에 동일 값 에코**(중복계상 아님, 계도 단일값). 전송0 8603에 값 오는 건 **통합처리** 탓. 도구 영향 0(공유컬럼 `RT_R_LAW_CLAUS30`이라 대조 일치, 팬텀행은 `filterByInput`로 숨김). `mapping/2025.ts` 조특법30조 주석에 기록.
- **✅ 1번(신규 3그룹) 검증 종료** — 2025 전체실행: 세액감면(9)·보장성보험료(416) 전원 일치, 교육비(192) 유일 불일치 **이수민 Y202500503 −1원**(원단위 절사: 국세청 8735 구분별 ×15% 절사 후 합 vs YTS 총액 기반). 정렬·배선 정상.

### 혼인세액공제(8790) 심층 분석 — 설계논쟁 (미완, 내일 실측)
- **UI 계약 재확인:** 국세청 혼인 팝업 = **체크박스 + 고정 500,000(회색 수정불가)** → `incDdcNfpCnt=1 + ddcAmt=500,000` 전송, OUT=500,000. 우리 전송(`ddcAmt=RT_MRRG`)이 UI와 동일.
- **★상규님 통찰(원본전송 우선):** `RT_MRRG`는 **한도후(소진후) 값** → 이걸 보내면 소진 단계가 **검증 사각**. 국세청이 캡해서 맞은 건지 우리가 캡한 값을 에코해 우연히 맞은 건지 **구분 불가**. **원본 500,000을 보내야** 국세청이 소진을 독립 재현→소진지점·소진액 검증됨. 완성까지 기다릴 것 없이 **지금부터 원본 지향**(현재 무영향이라 안전, 아래).
- **관문 = 국세청 소진캡 미확인:** "미검산(금액 스스로 안 만듦)"은 확인, **혼인 ddcAmt에 소진캡(MIN 잔액) 적용하는지는 미확인**. 캡하면 원본전송 성립, 맹목에코면 무의미.
- **실데이터:** 2025 혼인자 **8명 전원 500,000·소진 0명**(잔액 최소 2.3M) → RT_MRRG≡500,000이라 **현재 무영향**, 500,000 전환 안전. 혼인은 **근로소득세액공제 바로 다음** 순서라 잔액=산출(8990)−근로(8700). Finder=`docs/marriage-cap-find.mjs`(캐시기반, DB 없이).
- **별건(YTS 엔진 로깅버그):** 혼인 블록 `entered` 미초기화 → personCnt=0(대다수)이면 직전 근로세액공제 `RT_WIA`가 혼인 실행로그에 새어듦. **세액 무영향(로깅만)**, `onLoggingOfBalance_TAX` 내부 확인 필요. **엔진 소스(이 레포 밖)**.

### ▶ 다음 할 일 (내일 = 결정 A: 합성 캡 테스트)
1. **★혼인 소진캡 실측(합성):** `node docs/hometax-capture-io.mjs` → #1 **총급여 20,000,000**(잔액≈27만) 계산 → #2 **혼인 체크** 적용·계산 → Ctrl+C → `--parse`. **판정:** `8790` OUT이 잔액(`8990−8700`)으로 캡되나(≈27만) vs 500,000 그대로.
   - **캡 확인시** → 혼인 전송 `RT_MRRG→500,000` 원본 전환(소진자 배지로 노이즈 설명) + 보험료·교육비 등 한도후 전송 원본화 검토([[project_hometax_calc_backlog]] 원본전송 우선).
   - **맹목에코면** → 원본전송 무의미, 현 RT_MRRG 유지.
2. 검증 진행표 남은 그룹(기타세액공제 8751/8752/8753 등) 이어가기.
3. 백로그(인적공제 OUT 갭·집계형 OUT 매핑 한계·yts↔amtClusCd 분리매핑·계약변경 감지기).

### ⚠️ 주의
- DB 연결 간헐 끊김(ORA-12170) 재발 — `marriage-cap-find.mjs`가 DB 타임아웃나서 **캐시 단독 finder로 우회**함(위 8명 결과). 복구감시 `docs/db-ping.mjs`.
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout) — 무관.
- 방법론 고정: NTS 계약은 **라이브캡처**(`hometax-capture-io.mjs` → `--parse`), 매 세션 새 방법 금지.

---

## 이전: 2026-07-23 (origin/master = `d5ecce0` 이후 이 핸드오프 커밋)

### 이번 세션 한 것 (push 완료)
| 커밋 | 내용 |
|---|---|
| `7be531b` | 현황표 원천 테이블명 접두(CALC./MAIN.)·sticky 헤더 불투명 + 부양가족 유형별 실측 주석 |
| `008856d` | 8312 주택임차 원리금-거주자 원천 정정(PAY_WRK_MAIN → PAY_WRK_RENT_HABT_SPEC B0 SUM(PNINT_SUM)) |
| `ea4df2e` | 장기주택저당 injectHousingVals 주석 라벨 정정(8324~29, 카탈로그 수기오류 제거) |
| `c249495` | 전체실행 처리순서=화면 정렬순(sortItems 공통·클라서버, 12 route 배선) |
| `4781912` | 실행과정 드로어 '기타(계산과정 로스터 밖)' 구분행 강조 |
| `06e3cc1` | 기타 탭에 세액감면 그룹 추가(그밖의소득공제 아래) |
| `d5ecce0` | 기타 탭에 보장성보험료·교육비 그룹 + 교육비 소계 YTS공제 연결 수정 |

### 핵심 결과물
- **★부양가족 유형별 실측 확정**([[reference_nts_family_type_8004]]): **H1 기본공제는 유형 무관**(전원 한 유형에 몰아도 과세표준 동일), **H2 자녀공제(8763)는 직계비속(8005) 의존**(8005=0이면 8763=0). → 유형별→통합 단순화 **금지**(FMLY 쓰는 이유는 기본공제 아닌 자녀공제 재현). 3명 실측 `docs/hometax-family-lump-probe.mjs`, 실수방지 주석 2곳(2025.ts·runCompareForCalcNo.ts).
- **X202600522 자녀공제 규명**: YTS 319,370 vs 격리probe 550,000 차이 = **산출세액 소진**(정상, YTS 한도 정확적용). 세액공제 검증은 전체전송으로만 판단(격리 probe는 소진 못봄).
- **현황표 UI**: yts IN/OUT에 소속 테이블 접두(`ytsSrcWithTable` 공용화·미사용 `ytsInOf` 제거), IN 컬럼 값 흑색, 매핑/로스터 sticky 헤더 불투명(bg-muted, border-collapse 대비 th별 배경), 기타 구분행 강조.
- **8312 원천 정정**: 전송값을 `PAY_WRK_MAIN.HOUSE_RALR_HABT` → `PAY_WRK_RENT_HABT_SPEC`(RENT_HABT_CLS='B0') `SUM(PNINT_SUM)`. runCompareForCalcNo·housingList 양쪽. **8311(대출기관)은 MAIN.HOUSE_RALR_LENDER 확정**.
- **★전체실행 정렬순 일치**: `lib/sortItems.ts` 공통 comparator(클라 `useSortedList`·서버 `streamCompareBatch` 공유), 정렬상태 `listSort` 상위 리프트(전 탭 공유·기본 이름순), `runItemBatch`가 sortKey/dir을 배치 URL에 전달, 배치 route 12개 파싱→정렬. 화면 위→아래 순차 진행 시각화.
- **★기타 탭 신규 그룹 3개**: **세액감면**(그밖의소득공제 아래, `getTaxCutItems`, IN=8601 TAX_GOVM_AGREE/나머지 FN_PAY_GET_WRK_NTAX Txx, `filterByInput`=전송>0만), **보장성보험료**(혼인자녀출산 아래, `getInsuranceItems` 8710 12%·8711 15%), **교육비**(보장성보험료 아래, `getEducationItems` 소계형 8735 전용조회). 각 batch route(cut/insurance/education) + list 분기 + `ETC_GROUPS`/탭 등록.
- **교육비 8730 resultCol=RT_EDU_AMT 추가**: ③표 8735 소계 YTS공제 빈칸 수정 — 없으면 `ytsDdcMap[8735]` 미채움(카드 8431→8430 대칭).

### 검증 진행표(상규님 진행)
- 인적공제~그밖의소득공제 항목별 ytsCol/resultCol의 **field·table** 대조표 작성 중. 8312(SPEC B0)·8005(자녀공제 열쇠)·투자조합(PEN_SAVE_SUB_AMT 집계) 등 원천 확정. 남은: 기타세액공제 등.

### ▶ 다음 할 일
1. ~~신규 3그룹(세액감면·보험료·교육비) 앱 동작 수동확인 + 전체실행 정렬순 확인~~ **✅ 종료(2026-07-24, 2025 전체실행)** — 세액감면(9명)·보장성보험료(416명) 전원 일치, 교육비(192명) 유일 불일치 **이수민 Y202500503 −1원**(원단위 절사: 국세청 8735는 구분별 ×15% 절사 후 합, YTS는 총액 기반 → 1원 덜, 소스버그 아님). 진단 `docs/edu-diag.mjs`. 정렬·배선 정상.
2. ~~세액감면 8603(전송0)에 NTS공제 값 온 현상~~ **✅ 종료(2026-07-24)** — 라이브캡처(#3)로 **국세청 통합처리 실측확정**: 8608(90%)에만 감면대상급여 입력해도 국세청이 감면세액(225,585)을 8603·8608·8924(세액감면계)에 **동일 값 에코**(중복계상 아님, 계도 단일값). 우리 도구 영향 0(8603/8608 공유컬럼 `RT_R_LAW_CLAUS30`이라 대조 일치, 팬텀행은 `filterByInput`로 숨김). 근로세액공제 연쇄축소·결정세액 검산도 완전 정합(225,585−35,640=189,945).
3. 검증 진행표 남은 그룹(기타세액공제 등) 이어가기.
4. 백로그 다수: **인적공제 OUT 갭**(OUT_GROUPS에 "인적공제" 누락→현황표 OUT 전부 —)·**집계형 OUT 매핑 한계**·**self-subtotal 렌더 갭**(투자조합 개별이 소계 아래 안 붙음)·**yts↔amtClusCd 분리매핑**·**계약변경 감지기**.

### ⚠️ 주의
- **세액감면 RT_R_LAW(8개)·RT_R_LAW_CLAUS30(8603/8608) 공유 → 개별 대조 한계**. `filterByInput`로 전송>0 항목만 표시해 오표시는 완화했으나, 근본은 YTS가 여러 감면유형을 한 컬럼에 합산하는 구조.
- 신규 그룹 대상자 연도 주의(X2026/Y2025) — 화면 기본 2025.
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout) — 무관.

---

## 이전: 2026-07-22 (origin/master = `2895bf4`)

### 이번 세션 한 것 (push 완료)
| 커밋 | 내용 |
|---|---|
| `2619be5` | 세액감면 조특법30조 실측확정 배선 + 세액감면 7항목 전송 확대 |
| `f8becfa` | 인적공제 대조 4종 활성화(배우자·부녀자·한부모·부양가족) |
| `78fe37e` | 연도 선택 UI 개선 — 국세청 모의계산 연도 중심축화 |
| `f42bba7`~`2895bf4` | (후반) 실행과정 ②③·현황탭 정비 + 목록 정렬·열재배치·UX·스타일 (아래 상세) |
| (07-21) `df789af`~`ecdaaa9` | ③표 계산과정순 정렬·8407 통일·투자조합8410·연금건보고용 OBJ전송 (상세는 메모리) |

### 핵심 결과물
- **★조특법30조 세액감면 실측확정·검증·배선(어제 큰차이 3명의 유일 원인 해소)**: 라이브캡처(`docs/hometax-capture-io.mjs`)로 국세청 계약 확정 — 조특법30조=**"중소기업 취업자 감면", 율 70%(8603)/90%(8608) 두 가지뿐**(구 8604·8605·8916 오매핑 제거). 국세청 화면 "**감면대상급여 입력→감면세액 자동계산**"(감면세액=산출세액×(감면대상급여/총급여)×율). **근로소득세액공제 연쇄축소도 국세청이 재현**. 원천=`FN_PAY_GET_WRK_NTAX(CALC_NO,'MAIN'/'SUB',NULL,'Txx')` 합(T12=70%/T13=90%)→`CUT_` 가상컬럼 주입. **9명 원단위 검증 + 라이브검증 X202600518 결정세액·근로세액공제 0/104,954 완전일치**.
- **세액감면 7항목 전송 확대(원천 확보, X2026 대상0 미검증)**: 소득세법 8601(`MAIN.TAX_GOVM_AGREE`), 조특법30조제외 외국인기술자 8602/8612(T01/T02)·성과공유 8609(T30)·성과보상기금 8617/8610/8616/8614(T42/T40/T43/T41, 율 상규님확정)·우수인력복귀 8611(T50), 조세조약 8606(T20). `injectTaxCutVals`(runCompareForCalcNo)에서 FN 다중 Txx 조회+주입. 코드-화면 1:1은 라이브캡처 #2 useAmt 실측.
- **인적공제 대조 4종 활성화**: 배우자8002(`BASC_SUB_MATE_AMT`)·부녀자8103(`ADD_SUB_LADY_AMT`)·한부모8104(`ADD_SUB_SNGL_PRNT_AMT`)=self 대조. **부양가족은 ★국세청 OUT이 8004~8009 개별이 아니라 8003(통합)에 회신**(라이브 X202600219: 5명 7,500,000) → 8003 소계형 resultCol=`BASC_SUB_FAMILY_AMT`.
- **연도 선택 UI 개선**: 국세청 모의계산 연도를 맨 앞 드롭다운(**2025 기본/2026 선택가능**)+중심축으로, YTS 데이터 연도는 자동 연동(비활성 배지, 수정불가). **2026은 국세청 미개시라 "아직 제공되지 않습니다" 안내**(개시되면 `NTS_AVAILABLE`에 "2026" 추가로 풀림). 상수 `NTS_SELECTABLE`/`NTS_AVAILABLE`, 구 `CUR_YEAR`·`YEAR_OPTIONS`·`NTS_YEARS` 제거.

#### (후반) 실행과정 드로어·현황탭·목록 UI 정비
- **★실행과정 ②③·현황탭 역할 정리(오래 혼란스럽던 관계 확정)** → [[project_hometax_status_drawer_relation]]. **현황탭=기준정보**(매핑 현황 + 계산과정 순서 로스터, 둘 다 DetailPanel 2패널). **실행과정 ③=로스터(계산과정 대조) 순서**·**②=매핑현황(원천 확인) 순서**. ②표=보낸값의 **YTS 원천컬럼(`MAIN.`/`CALC.` 접두, 그 외 테이블·함수 그대로)** 표시 / ③표=YTS↔NTS 대조(보낸값·받은값·YTS공제·판정). 소계 chevron 접기·전체/값보기 토글·행 클릭선택(청색)+hover·헤더 더블클릭 최대화. 현황 각 제목행에 소스(`mapping/2025.ts › MAPPING_2025`/`PROC_LABEL_CODE_2025`)·역할 표기.
- **목록 UI 정비**: 7개 테이블 열 정렬(공통 `useSortedList`+`SortableTh`, **기본 이름 오름차순**), 열 순서 **이름·사번·CALC_NO**, **실행/분석**·**계산/소진지점** 열 분리, 표준·중도퇴사 흐린적색, `workStatusLabel` '계속근로/중도퇴사', **기타 탭=드롭다운 선택 후 조회**(트리거 클릭 시 자동조회 제거), 기타 그룹(그밖의소득공제/기타세액공제) **구성 표시**(①드롭다운 부제 ②테이블 캡션, MAPPING group 파생 — 8751~53 group `세액공제`→`기타세액공제`, OTHER_INCOME은 `OTHER_INCOME_CODES`로 좁힘). 현황·목록 스타일=제목행 흰색·열제목 흐린회색·리스트 흰색.

### ▶ 다음 할 일
1. **★원천 검증 (상규님 다음 계획 = 최종목표)** — "보내는 값이 YTS 어디서 오는지"를 검증. ②표가 원천을 눈으로 보여주니, 이를 **자동 검증**으로 굳힌다: ①기술검증(각 항목이 매핑 `ytsCol`에서 값 가져가 보내는가 — 유닛테스트) ②도메인검증(그 `ytsCol`이 맞는 원천인가 — 상규님 지식+실측). [[feedback_maintainability_first]]
2. **정합성 테스트(백로그)** — `MAPPING_2025`↔`PROC_LABEL_CODE_2025` (미등록0·중복없음·계산과정 라벨 커버). **국소 갱신 리팩터 때 함께.**
3. **연도 지원 구조 리팩터(백로그)** — 현재 `mapping/2025.ts` 하드코딩(9곳 import). `getMapping(year)` 로더+연도분기, `2026.ts=2025복사+개정행만`. **1차 완성 후 집중**(상규님: 유지보수가 플래그십·최종목표).
4. 부양가족 8003 라이브재확인(2025 대상자)·세액감면 확대분 값검증(대상0).

### ⚠️ 주의
- **화면 기본이 YTS 2025↔국세청 2025로 통일됨.** 오늘 검증한 데이터는 X2026(2026귀속)이라 지금 화면 기본(2025)에선 안 보임 — 배선 코드는 연도무관(calcNo 기반)이라 동작 동일, 라이브 재확인은 2025 대상자로.
- **개별 행 실행은 캐시에 저장 안 됨**(전체실행만 저장) — 개별 결과는 실행과정 드로어에서 확인. 캐시 기반 검증은 전체실행 후.
- DB 연결 간헐 끊김(ORA-12170) 재발 — 복구감시 `docs/db-ping.mjs`.
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout) — 무관.
- 검증 스크립트: `docs/clause30-diag.mjs`(세액감면), `docs/personal-ded-diag.mjs`(인적공제).

---

## 이전: 2026-07-20 (밤, origin/master = `37be575`)

### 이번 세션 한 것 (2커밋, push 완료)
| 커밋 | 내용 |
|---|---|
| `3ade913` | fix: 세대원 주택마련저축 국세청 전송 0처리 |
| `37be575` | feat: 전체비교에 중간 계 차이컬럼 7개 추가 |

### 핵심 결과물
- **NTS 응답 계/소계 OUT 코드 실측 확정** (라이브 캡처 값 역산 — 응답 181코드 오나 한글명 없어 값 구조로 식별): **8916 차감소득금액 · 8920 특별소득공제계(주택자금 포함) · 8921 그밖의소득공제계 · 8922 특별세액공제계 · 8923 세액공제계 · 8924 세액감면계 · 8770 표준세액공제**(특별공제 방식 선택 시 0=택1 배타). 결정세액 항등식(산출−감면계−세액공제계=결정)으로 교차검증. 8920은 보험료만/주택자금만 각각 8920에 합산돼 "보험료소계 아닌 전체 특별소득공제계"로 확정.
- **전체비교에 중간 계 차이컬럼 추가**: 산출/결정만 비교하면 중간 계가 어긋나도 상쇄로 안 보이는 문제 해결. 목록표 비교영역을 항목당 1컬럼 **`YTS값 (차이)` 7컬럼**으로 통일 — 특별소득공제·그밖의소득공제·차감소득금액·산출세액·세액감면·세액공제·결정세액. 차이=NTS−YTS(0=정상 회색·≠0 빨강·미실행 —). 기존 NTS별도컬럼+✓✗아이콘 제거. YTS원천 SPCL_SUB_AMT_SUM/OTO_SUM/BIA_AMT/PROD_TAX_AMT/TAX_CUT/RT_SUM/RES_INCM_TAX, NTS는 ntsMap. 변경 `allList.ts`+`HometaxCalcPanel.tsx`(calcCell). 엑셀 미반영(화면만).
- **세대원 주택마련저축 0처리**: HOUSE_HLDR_YN='2'면 국세청은 세대구분값 없어 납입액 보내면 ×40% 자체공제→YTS(미공제 0)와 불일치. `buildCompareInput`에서 OTHER_8403/8404/8405에 0 주입(`applyHouseMemberSavingsRule`, mainRow에 HOUSE_HLDR_YN 추가).

### 라이브 검증 (2025 485명, 완료)
- **10명 차이 전원 원인규명, 소스버그(C) 0**:
  - 7명 = 결정세액 ±1 = 원단위 절사(정상, 무시)
  - **3명 큰차이 = 세액감면(조특법30조) 미전송** (Y202500502 928,793 · Y202500518 1,341,008[교육비소진] · Y202500546 779,728). DB 대조: 세액감면 차이 = TAX_CUT 전액(전부 RT_R_LAW_CLAUS30)과 정확 일치, NTS=0(우리가 안 보냄, 매핑 8601~8606 send:false). 결정세액 차이도 산술 완전검증(감면 미전송분 − 세액공제 산출한도 연쇄증가분). 세액공제(차이)는 독립버그 아닌 연쇄.
- **★차감소득금액(BIA_AMT↔8916) 우려했던 정의차 없음 — 전원 diff 0 완벽일치** (기우였음).

### ▶ 다음 할 일
1. **세액감면 조특법30조 `send:true` 배선** — 감면율(100/90/70/50)별 4코드(8604/8605/8608 등) 분할이라 라이브캡처로 NTS계약(어느 코드) **먼저 확정** 후 배선. 이러면 큰차이 3명 일치.
2. **세대원 0처리 값검증** — 세대원 대상자 전체실행해 주택마련저축 diff 0 확인.
3. (선택) 계 코드를 `NTS_FLOW`(HometaxCalcPanel)에 라벨 등재 — 현재 실행과정 전체표에서 코드로만 표시. ※`NTS_RESULT_CODES`(mapping)는 실제 미사용 중복(죽은 코드).
4. (이월) 그밖의소득공제·기타세액공제 값검증 / baseline 감지기(두 얼굴 ②).

### ⚠️ 주의
- **전체비교 계 컬럼값은 재실행해야 채워짐**(기존 캐시엔 계 없음) — 계가 "—"면 그 건 재실행(▶).
- GCM 계정 선택 프롬프트 제거함: `credential.https://github.com.username = kim-sanggyu` (되돌리기: `--unset`).
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout·.next/types) — 이번 작업과 무관.

---

## 이전: 2026-07-20 (낮, `f9ca9a8`) — 실행과정 드로어 재구성

### 한 것 (1커밋, push 완료)
| 커밋 | 내용 |
|---|---|
| `f9ca9a8` | 실행과정 드로어 3분할 재구성(결과비교/전송입력/IN-OUT) + 기납부세액 제거 + 8923→8700 코드정정 |

### 핵심 결과물
- **실행과정 드로어 재구성**: ①결과비교 ②전송한 공제입력 ③NTS원본IN/OUT 3개 접이식 패널(`DetailPanel`). 단일클릭=개별토글, **더블클릭=그 패널만 최대화(`flex-1`)하고 나머지는 헤더만 접힘**(`focusedPanel` 상태). 각 패널 독립 세로/가로 스크롤 + `thead sticky`로 헤더 고정. 좌(계산과정)-우(실행과정) 드로어 자체도 드래그 리사이즈 가능. 두 드로어(계산과정 단독/실행+계산 나란히) 모두 우상단에 전체보기 토글(사이드바 아이콘폭 3rem만 남기고 나머지 전체 차지).
- **①결과비교 범위 축소**: 총급여(8900)→결정세액(8999)까지만. **기납부세액(8991)·차감징수(8992)·지방소득세(8998) 관련 코드 전량 제거** — 레거시 `runHometaxCalc()`(총급여+기납부 팝업 방식) 함수, `route.ts`의 `mode:"simple"` 분기까지 통째로 삭제.
- **버그 정정(실측 확인)**: 근로소득세액공제의 NTS 코드가 **8923이 아니라 8700**이었음. 총급여 5천만원 단독 입력 라이브 프로브로 확인 — 8923은 "세액공제 합계"(8700+8770 표준세액공제 등)였고, 개별 근로소득세액공제 결과는 8700에 옴. `mapping/2025.ts`(NTS_RESULT_CODES) · `HometaxCalcPanel.tsx`(NTS_FLOW, PROC_LABEL_CODE, compareRows) 전부 수정.
- **소계형 outCode 배선**: 카드(8431~8463→8430), 의료비(8720/8721/8725/8729→8726)에 `outCode` 추가. 카드는 `resultCol:"OTO_CARD_ETC"`도 추가해 YTS쪽 소계 대조 가능해짐. ②패널에 그룹 마지막에 "카드 소계"/"의료비 소계" 행 별도 삽입.
- **②전송한 공제입력 단순화**: YTS컬럼·전송값(가공)·NTS반영(가공) 제거 → **NTS IN/OUT 원본값 그대로**(항목/NTS코드/보낸값/받은값 4열). "전체보기/값보기" 토글 추가(값보기 = 화면에 실제 보이는 보낸값·받은값 중 하나라도 있는 행만). YTS 대조는 ③패널 전담으로 분리.
- **드로어 데이터 조회 크로스탭 버그 수정**: 상세조회 드로어의 `detailRow`가 `allItems`(전체탭)에서만 찾던 걸, 기부금/카드/의료비/연금/기타 탭 리스트까지 다 뒤지도록 확장(`DetailRowLike`). 전체탭 안 들어가고 다른 탭에서 바로 열어도 계산과정이 정상 표시됨.

### 발견한 이슈 (코드 버그 아님 — 데이터/외부 프로세스)
- **X202600103**: `PAY_WRK_GIFT_ADJ`가 이 건에 대해 완전히 비어있어 기부금 전체(정치자금·고향사랑·일반기부금 등)가 국세청에 미전송 → 결정세액 434,999원 차이(빠진 공제액 합과 정확히 일치). hometax-calc 코드(`injectGiftVals`)는 정상 동작 — 있으면 보냄. 원인은 `PAY_WRK_GIFT_ADJ`를 채우는 후속 "확정" 배치가 이 calcNo에 대해 안 돌았거나 누락된 것으로 추정. 이 Next.js 앱 밖의 별도 프로세스라 여기서 고칠 수 없음 — 배치 담당 쪽에 문의 필요.

### ▶ 다음 할 일
1. **값검증** — 그밖의소득공제·기타세액공제 그룹 대상자로 전체실행, 원단위 일치 확인(특히 한도캡 240/1500/1000만 초과 시 NTS 자체캡 동작). (이전 세션에서 이월)
2. **계산과정 라벨맵 확장** — `PROC_LABEL_CODE`에 아직 남은 소계형(카드·의료·교육·기부·투자조합) 편입 → 계산과정 라인 하이라이트·순서 확대. (근로소득세액공제는 이번에 8700으로 정정 완료)
3. **X202600103 기부금 미전송 건** — `PAY_WRK_GIFT_ADJ` 확정 배치 담당자에게 문의.
4. baseline 감지기(도구의 "두 얼굴 ②"). (이전 세션에서 이월)

### ⚠️ 주의
- **새 필드(`YtsResult.workTax/workAmt/taxBase/wiaCredit`, `CompareRunResult.yts` 확장분)는 재실행해야 채워짐** — 기존 캐시엔 없어서 "①결과비교" 중간행이 "—"로 보이면 그냥 그 건만 다시 실행(▶)하면 됨.
- 8751·8752·8753은 X2026 대상자 0명이라 원단위 미검증 (이전 세션부터 미해결).
- `npm run typecheck` 기존 에러 4개(tax-insight·hwp-layout·.next/types) — 이번 작업과 무관, 원래 있던 것.
- 프로젝트 루트에 있던 중복 클론 `YTS_TOOL/YTS_TOOL/`(자체 .git 포함, 구버전)는 이번 세션에 삭제함 — 더 이상 신경 안 써도 됨.

### 방법론(고정)
- NTS 계약 분석은 **라이브 캡처**로: `node docs/hometax-capture-io.mjs`(입력·계산) → `--parse`(증분 diff). 상세 `docs/nts-contract-capture-method.md`. 매 세션 새 방법 만들지 말 것.
- 컬럼 존재/테이블 확인은 일회성 스크립트 + `node --env-file=.env.local <script>.mjs`.

### 이어가기 절차 (다른 PC)
1. `git pull` (origin/master = `37be575` 이상)
2. 이 문서 읽기
3. `npm run dev`로 화면 확인 시 로그인 필요(아이디/비번)
